# Frontend (safe to expose)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
VITE_CAATH_EMAIL_OTP_ENABLED=false

# Do NOT expose any service-role or encryption keys in VITE_ variables.
# Keep server/edge secrets only in Supabase Edge Function secret store.
